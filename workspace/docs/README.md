# workspace docs

这里放 `workspace/` 下的长期设计文档。

`workspace/` 根目录只保留 agent 运行入口和本地 skill：

- `AGENTS.md`
- `.agents/skills/`

架构、施工、历史版本和后续设计演进统一放在本目录，避免根目录同时出现
`ARCHITECTURE.md`、`CONSTRUCTION.md` 和版本文件造成混乱。

当前主要文档集：

- `architecture-construction/`
  架构手册与施工手册的版本化归档和当前版本。

推荐阅读：

1. `../AGENTS.md`
2. `architecture-construction/v3/ARCHITECTURE.md`
3. `architecture-construction/v3/CONSTRUCTION.md`
