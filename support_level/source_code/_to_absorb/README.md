# _to_absorb

这里是旧系统源码资产的待拆区。

放进这里，表示：

- 东西先保留了
- 但当前不承认它已经是合理的长期结构
- 也不默认让 `compile` 直接把它当成稳定源码根

这里常见的东西包括：

- 整套版本快照
- 集成 workspace
- 只是兼容入口的 alias
- 未展开或未归类的源码包

处理原则：

- 后续按旧 skill 一条条过
- 识别它实际对应哪些源码模块
- 能拆的拆进 `../modules/`
- 不能证明长期价值的，删掉
