# code_assets

这里放本地长期保留的代码资产。

这层再分成两类：

- `projects/`
  单个源码项目资产，默认按标准源码基线管理
- `workspaces/`
  只有整体协同才有意义的工作区资产

这层只负责回答：

- 本地有哪些代码资产
- 它们分别属于源码项目，还是工作区
- 它们真实放在什么位置

如果只是查代码资产位置或角色，先留在这层。
如果任务已经变成“我要编什么”，回 `../compile_targets/` 或 `compile`。
