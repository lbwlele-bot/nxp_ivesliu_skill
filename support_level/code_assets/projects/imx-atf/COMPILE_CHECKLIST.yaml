# ATF 项目编译清单模板。AI 只填写 TBD 和本轮 intent；命令由 profile 生成。
schema_version: 1
kind: project_compile_checklist
project: imx-atf

case_root: TBD

source:
  ref: TBD

parameters:
  platform: TBD
  spd: TBD

inputs:
  artifacts: []
  files: []

toolchain:
  make: /usr/bin/make
  compiler: toolchain/arm-gnu-toolchain-14.3.rel1-x86_64-aarch64-none-linux-gnu/bin/aarch64-none-linux-gnu-gcc

intent:
  action: rebuild
  reason: TBD

build:
  mode: isolated_git

outputs:
  bl31:
    type: nxp.atf.bl31
    collect_from: build/${parameters.platform}/release/bl31.bin
    publish_to: artifacts/imx-atf/bl31.bin
