schema_version: 1
target: imx-atf

rules: {}
command_rules: {}

execution_rules:
  imx-atf-isolated-source:
    component: imx-atf
    mode: isolated_git
    source_kind: managed_git
    reason: ATF writes build output below the source tree; the managed source fingerprint must remain immutable.
