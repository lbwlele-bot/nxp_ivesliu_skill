# android

这里放 Android 相关源码发布包。

它作为一个源码项目保留在 `code_assets/projects/` 里，
是因为后续如果涉及 Android，通常先关心“本地有没有这个项目的源码包”，
而不是先进入某个集成工作区。

当前保留资产：

- `imx-android-16.0.0_1.4.0.tar.gz`

使用规则：

- 先确认当前任务是否真的落到 Android
- 需要看本地是否已有 Android 发布包时，先查这里
- 需要解压、修改、构建时，复制到 `../../../work/<case>/` 再做
