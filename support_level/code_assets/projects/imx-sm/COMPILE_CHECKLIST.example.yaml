# 结构示例，不是已验证 case，也不应直接执行。
schema_version: 1
kind: project_compile_checklist
project: imx-sm

case_root: /home/ives/桌面/NXP_v2/support_level/work/example-imx95-flashbin
source:
  ref: lf-6.18.2-1.0.0

parameters:
  config: other/mx95rte

inputs:
  artifacts: []
  files: []

toolchain:
  make: /usr/bin/make
  compiler: toolchain/arm-gnu-toolchain-14.3.rel1-x86_64-arm-none-eabi/bin/arm-none-eabi-gcc

intent:
  action: rebuild
  reason: 使用已经由软件栈选定的 RTE 配置生成 m33_image.bin

build:
  mode: isolated_git

outputs:
  smfw:
    type: nxp.smfw.image
    collect_from: build/${parameters.config}/m33_image.bin
    publish_to: artifacts/imx-sm/m33_image.bin
